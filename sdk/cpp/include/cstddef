#pragma once
// Freestanding shim: wraps the clang builtin <stddef.h> and exposes types in std::.
// Only the subset needed by abi.hpp / bot code is provided.
#include <stddef.h>
namespace std {
using size_t    = ::size_t;
using ptrdiff_t = ::ptrdiff_t;
using nullptr_t = decltype(nullptr);
} // namespace std
